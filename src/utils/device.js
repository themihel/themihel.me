const size = {
  screenSmMin: '576px',
  screenMdMin: '768px',
  screenLgMin: '992px',
};

export const device = {
  screenSmMin: `(min-width: ${size.screenSmMin})`,
  screenMdMin: `(min-width: ${size.screenMdMin})`,
  screenLgMin: `(min-width: ${size.screenLgMin})`,
};
